# Local Signing Materials

Signing certificates, profiles, keystores, and passwords are intentionally excluded from this repository.

Configure a local HarmonyOS signing profile in DevEco Studio before building an installable HAP. Keep the generated files outside the repository or under this directory; Git ignores them. Never place signing passwords or private keys in `build-profile.json5`, source code, documentation, or issue reports.
